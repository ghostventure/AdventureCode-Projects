# ExcelBolt Handoff Notes - 2026-04-17

## Current Live Target

- Firebase project: `excelbolt`
- Live URL: `https://excelbolt.web.app`
- Main app directory: `/home/sniper-lion-main/Documents/ExcelBolt`

## What Changed

- Added a photo-led public landing page for signed-out visitors at `/` and `/landing`.
- Kept the existing account screen separate at `/signin`, `/login`, and `/auth`.
- Landing copy now explains ExcelBolt in plain language:
  - connect business apps
  - pick a report
  - export a clean Excel workbook
  - start a 30-day free trial
- Landing buttons route into the existing sign-in/sign-up screen with the correct auth tab selected.
- Added logged-in landing-page recovery with an `Open workspace` path.
- Fixed a production runtime issue caused by circular Vite vendor chunks. The previous bundle could build but fail in the browser before React mounted.
- Updated Vite manual chunks so React, Firebase, and other vendor code are separated without a circular dependency.
- Updated the service worker so it only intercepts same-origin requests.
- Updated Firebase Hosting CSP so the landing-page Unsplash images and Google-hosted fonts can load cleanly.
- Tightened mobile landing CSS so the hero, buttons, and proof cards fit phone screens without horizontal scrolling.

## Important Files

- `excelbolt.jsx`
  - public landing component, route handling, sign-in route behavior, mobile landing CSS
- `vite.config.js`
  - corrected manual chunk logic
- `public/sw.js`
  - service worker cache version and same-origin fetch guard
- `firebase.json`
  - CSP update for image/font loading
- `docs/PROJECT_STATUS.md`
  - current state summary

## Verification Completed

- `npm test` passed.
- `npm run build` passed.
- `npx firebase-tools deploy --only hosting --project excelbolt` passed.
- Live smoke checks confirmed:
  - `https://excelbolt.web.app/` renders the public landing page.
  - `https://excelbolt.web.app/landing` renders the public landing page.
  - `https://excelbolt.web.app/signin` renders the account screen.
- Desktop and phone screenshots were checked after deploy.

## Recovery Notes

- If the live site ever returns a blank page after a clean build, check browser console output for vendor chunk/runtime errors before changing app code.
- Do not re-add `splitVendorChunkPlugin`; it was part of the circular chunk problem.
- Keep the service worker same-origin only unless the CSP is updated deliberately.
- If a browser still shows an older shell after deploy, clear site data or unregister the previous service worker once, then reload.
