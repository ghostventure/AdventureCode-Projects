# Deploy Checklist

- `npm run lint`
- `npm run audit`
- `npm run build`
- `npm run smoke`
- `npm run deploy:rules`
- `npm run deploy`
- Verify live `/`, `/health`, `/api/health`, `/robots.txt`, and `/sitemap.xml`.
- Document live URL and deploy timestamp in `PROJECT_NOTES.md`.
