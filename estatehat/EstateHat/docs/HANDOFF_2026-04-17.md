# EstateHat Handoff Notes - 2026-04-17

## Current Live Target

- Firebase project: `estatehat`
- Live URL: `https://estatehat.web.app`
- Main app directory: `/home/sniper-lion-main/Documents/EstateHat_files/estatehat-clean`

## What Changed

- Added a photo-led public landing page, now served through `/start.html`.
- Split account access into its own `/signin.html` HTML entry instead of sharing the main site shell.
- Kept `/` as a small redirect page to `/start.html`.
- Kept the signed-in app shell at `/home.html`.
- Landing copy now explains EstateHat in plain language:
  - no listing fee
  - guided forms included
  - one account for listing, messages, forms, reminders, and closing steps
  - Hat Data and Goodies as helper surfaces
- Added logged-in landing-page behavior so a signed-in user who visits `/landing` can open the app instead of getting stuck in auth.
- Added EstateHat-specific imagery using home, paperwork, and family-home visuals.
- Installed the Goodies workspace with 94 EstateHat-specific helper features.
- Renamed user-facing product labels:
  - `UX/UI` and `94 installable UX/UI Features` became `Goodies`.
  - `50 installed workflow components` became `Hat Data`.
  - `Boilerplates` became `Forms` because that word is easier for normal users.
- Updated Assistant, Help Center, FAQ & Scope, Terms, Privacy, footer links, and Forms language so Hat Data, Goodies, and Forms are consistent.
- Clarified in legal/support copy that Hat Data and Goodies are product-interface labels only and do not create legal, brokerage, title, escrow, lending, tax, appraisal, underwriting, investment, fiduciary, or advisory services.
- Optimized EstateHat for both phones and desktop:
  - safer phone spacing
  - larger touch targets
  - mobile command-palette placement
  - wider desktop page sizing
  - overflow guards for dense rows and inline controls
- Added One EstateHat:
  - one shared account marker across listings, search, messages, forms, Hat Data, Goodies, documents, support, and closing steps
  - `users/{uid}` now stores `oneEstateHatId`
  - Firestore profile schema is now version `14`
  - detailed record: `docs/ONE_ESTATEHAT_ROLLOUT_2026-04-18.md`
- Installed Move Kit:
  - 50 new buyer, seller, document, closing, and trust helpers
  - separate from existing Hat Data and Goodies workspaces
  - added to navigation, command search, footer links, Assistant, Help, FAQ, Terms, and Privacy
  - detailed record: `docs/MOVE_KIT_50_INSTALL_2026-04-18.md`
- Installed final responsive OS layout pass:
  - operating-system detector marks the shell with OS and layout mode
  - desktop workspaces get overlap guards
  - mobile/iOS/Android/narrow screens stack major workspaces as tiles
  - public landing page grids use mobile-safe wrapping
  - Assistant includes a plain-language layout explanation
  - detailed record: `docs/RESPONSIVE_OS_LAYOUT_2026-04-18.md`
- Duplicated the EstateHat footer plates onto public screens:
  - landing page now has the same Brand, Product, Company, Legal, Contact, platform, trust, and disclaimer blocks as the signed-in app footer
  - sign-in/create-account page now uses the same public footer component
  - detailed record: `docs/PUBLIC_FOOTER_PLATES_2026-04-18.md`
- Added simple imagery throughout EstateHat:
  - shared image-band component
  - main signed-in page headers
  - sign-in/create-account page
  - Hat Data, Move Kit, and Goodies workspaces
  - detailed record: `docs/SIMPLE_IMAGERY_PASS_2026-04-18.md`
- Split sign-in from the rest of the site:
  - `start.html` is now the public landing HTML entry
  - `home.html` is now the signed-in app HTML entry
  - `signin.html` is now the auth-only HTML entry
  - `index.html` redirects to `start.html`
  - Vite builds all four HTML files
  - Firebase rewrites unknown routes to `start.html`
  - detailed record: `docs/SIGNIN_HOME_SPLIT_2026-04-19.md`

## Important Files

- `src/main.jsx`
  - signed-in app shell handling for `/home.html`
- `src/start.jsx`
  - landing-only React entry for `/start.html`
- `src/signin.jsx`
  - sign-in-only React entry for `/signin.html`
- `start.html`
  - public landing HTML entry
- `home.html`
  - signed-in app HTML entry
- `signin.html`
  - account-access HTML entry
- `index.html`
  - redirect entry to `/start.html`
- `src/MarketingLanding.jsx`
  - public EstateHat landing page and footer placement
- `src/AuthScreen.jsx`
  - sign-in/create-account page and footer placement
- `src/EstateHatPublicFooter.jsx`
  - shared public footer plates for landing and sign-in
- `src/EstateHatImagery.jsx`
  - shared simple image bands for public and signed-in screens
- `src/estatehat-platform-alpha.jsx`
  - shell navigation, Assistant, FAQ, Legal, Forms, Hat Data, and Goodies language
- `src/AdditionalComponents.jsx`
  - Hat Data workspace
- `src/EstateHatUxSuite.jsx`
  - Goodies workspace
- `docs/PROJECT_STATUS.md`
  - current state summary
- `docs/UX_UI_LAB_INSTALL_2026-04-17.md`
  - Goodies install, rename, Forms copy, and verification record
- `docs/ONE_ESTATEHAT_ROLLOUT_2026-04-18.md`
  - One EstateHat account model notes and recovery notes
- `docs/MOVE_KIT_50_INSTALL_2026-04-18.md`
  - Move Kit 50-item install record
- `docs/RESPONSIVE_OS_LAYOUT_2026-04-18.md`
  - responsive OS detector, desktop overlap guard, mobile tile layout, and smoke-test record
- `docs/SIGNIN_HOME_SPLIT_2026-04-19.md`
  - separate sign-in/home HTML entry split and deployment record

## Verification Completed

- `npm test` passed.
- `npm run build` passed.
- `npm run deploy:hosting` passed.
- Live smoke checks confirmed:
  - `https://estatehat.web.app/` renders the public landing page.
  - `https://estatehat.web.app/landing` renders the public landing page.
  - `https://estatehat.web.app/signin` renders the account screen.
- `npm test`, `npm run build`, `npm run sync:android`, and `npm run sync:ios` passed after One EstateHat.
- Hosting and Firestore rules were deployed together after One EstateHat.
- Live smoke confirmed the deployed bundle includes `One EstateHat` and `oneEstateHatId`.
- `npm test`, `npm run build`, `npm run sync:android`, and `npm run sync:ios` passed after the Move Kit install.
- `npm run deploy:hosting` passed after the Move Kit install.
- Live smoke confirmed the deployed shell and bundles include Move Kit, `move-kit`, `EstateHatMoveKit`, `Closing Confidence Table`, `Fair Housing Copy Guard`, and `estatehat-move-kit-v1`.
- `npm test`, `npm run build`, `npm run sync:android`, and `npm run sync:ios` passed after the responsive OS layout pass.
- Chromium desktop screenshot smoke passed against the built landing page, and bundle smoke confirmed the responsive OS layout strings are present.
- `npm run deploy:hosting` passed after the responsive OS layout pass.
- Live smoke confirmed `/`, `/signin`, the main bundle, Move Kit chunk, and Goodies chunk include the deployed responsive layout markers.
- `npm test` and `npm run build` passed after adding the shared public footer plates.
- `npm run deploy:hosting` passed after adding the shared public footer plates.
- Live smoke confirmed `/` and `/signin` return `200`, load `assets/index-DlQaW0Zl.js`, and the deployed bundle includes `Licensed Escrow Partners` plus `A Place Where You Can Hang Your Hat`.
- `npm test` and `npm run build` passed after the simple imagery pass.
- `npm run deploy:hosting` passed after the simple imagery pass.
- Live smoke confirmed `/` and `/signin` return `200`, load `assets/index-DOe3Gw2h.js`, and the deployed bundle includes `estatehat-image-band` plus the simple image alt text.
- `npm test` and `npm run build` passed after splitting sign-in and home entries.
- `npm run deploy:hosting` passed after splitting sign-in and home entries.
- Live smoke confirmed `/` redirects to `/start.html`, `/start.html` loads `assets/start-DnHcn0-y.js`, `/home.html` loads `assets/home-BOgexhMn.js`, and `/signin.html` loads `assets/signin-DK-XeVal.js`.
- `npm run build:windows` passed after the responsive OS layout pass.
- `wine32:i386` was installed and a clean 32-bit Wine prefix was created at `/home/sniper-lion-main/.wine32-estatehat`.
- `WINEPREFIX=/home/sniper-lion-main/.wine32-estatehat WINEARCH=win32 npm run dist:win` passed after the responsive OS layout pass.
- Windows outputs confirmed:
  - `release/EstateHat-Setup-1.0.0.exe`
  - `release/EstateHat-Setup-1.0.0.exe.blockmap`
  - `release/win-unpacked/EstateHat.exe`
- `./gradlew assembleDebug` passed from the Android project after the responsive OS layout pass.
- Android outputs confirmed:
  - `android/app/build/outputs/apk/debug/app-debug.apk`
  - `release/EstateHat-Android-Debug-1.0.0.apk`
- `npm run sync:ios` passed after the responsive OS layout pass.
- iOS output confirmed:
  - `ios/App/App.xcodeproj`
  - `ios/App/App/public`
- `.ipa` was not produced on this Linux environment because iOS archive/export requires macOS/Xcode signing and Apple provisioning.

## Recovery Notes

- Do not rename `Forms` back to `Boilerplates`; the old term was intentionally removed from user-facing copy.
- Keep Hat Data and Goodies legally scoped as product surfaces, not regulated services.
- Keep Move Kit separate from Hat Data and Goodies unless a later product decision intentionally merges them.
- Keep `data-layout-mode` and the `estatehat-*` responsive classes together; the mobile tile behavior depends on both.
- If public routing regresses, start in `src/main.jsx`.
- If wording regresses, search for `Boilerplates`, `UX/UI`, `94 installable`, and `50 installed` before deploy.
